# คำอธิบายการทำข้อสอบ Exit Exam MVC 2-2568

## 📌 ข้อที่เลือกทำ
**ข้อ 1: ระบบติดตามข่าวลือบนสื่อสังคมออนไลน์ (Rumor Tracking System)**

---

## 🔗 GitHub Repository
**Link:** https://github.com/kmitlProjects/MVC_2568_2_q1.git

---

## 📂 a) ไฟล์ที่สำคัญใน MVC และการทำงานร่วมกัน

### 🗄️ MODEL Layer (โฟลเดอร์ models/)
**หน้าที่:** จัดการข้อมูลและ Business Logic

1. **models/database.py**
   - เชื่อมต่อกับ SQLite database (rumor_tracking.db)
   - มี method: `get_connection()`, `execute_query()`, `fetch_all()`, `fetch_one()`
   - ใช้ร่วมกับ Model อื่นๆ สำหรับทุก query

2. **models/rumour.py** (RumourModel)
   - จัดการข้อมูลข่าวลือ
   - Method สำคัญ:
     - `get_all_rumours()` - ดึงข่าวลือทั้งหมดเรียงตามจำนวนรายงาน
     - `get_rumour_by_id()` - ดึงข่าวตาม ID
     - `update_status_to_panic()` - เปลี่ยนสถานะเป็น panic (>= 5 รายงาน)
     - `verify_rumour()` - บันทึกผลการตรวจสอบ
     - **`calculate_credibility_score()`** - คำนวณคะแนนจากสูตร: (รายงาน "น่าเชื่อถือ" / รายงานทั้งหมด) × 100
     - **`update_credibility_score()`** - อัปเดตคะแนนอัตโนมัติหลังมีรายงานใหม่

3. **models/report.py** (ReportModel)
   - จัดการการรายงานข่าวลือ
   - Method สำคัญ:
     - `create_report()` - บันทึกการรายงานใหม่
     - `check_duplicate_report()` - ตรวจสอบการรายงานซ้ำ (Rule 4.1)
     - `get_reports_by_rumour()` - ดึงรายงานทั้งหมดพร้อมข้อมูลผู้รายงาน

4. **models/user.py** (UserModel)
   - จัดการข้อมูลผู้ใช้
   - Method สำคัญ:
     - `get_all_users()` - ดึงผู้ใช้ทั้งหมด (13 คน)
     - `get_verifiers()` - ดึงเฉพาะผู้ตรวจสอบ (V001-V003)
     - `get_user_by_id()` - ดึงข้อมูลผู้ใช้ตาม ID

5. **database.py** (Script สร้างฐานข้อมูล)
   - สร้างตาราง: Users, Rumour, Report
   - เพิ่มข้อมูลตัวอย่าง:
     - 13 ผู้ใช้ (10 ผู้ใช้ทั่วไป: user001-user010 + 3 ผู้ตรวจสอบ: V001-V003)
     - 10 ข่าวลือ
     - 24 รายการรายงาน
   - **วิธีรัน:** `python database.py`

---

### 🎨 VIEW Layer (โฟลเดอร์ templates/)
**หน้าที่:** แสดงผลข้อมูลให้ผู้ใช้

1. **templates/index.html**
   - หน้ารวมข่าวลือทั้งหมด
   - แสดงข่าวเรียงตามจำนวนรายงาน
   - มี Badge แสดงสถานะ (PANIC, Verified)
   - แสดงคะแนนความน่าเชื่อถือ
   - ปุ่มดูรายละเอียด

2. **templates/detail.html**
   - หน้ารายละเอียดข่าวลือ
   - ประกอบด้วย 4 ส่วน:
     - **ส่วนที่ 1:** ข้อมูลข่าว + คะแนนความน่าเชื่อถือ + สูตรคำนวณ
     - **ส่วนที่ 2:** ฟอร์มรายงาน (ผู้ใช้ทั่วไป) - แสดง [รหัสผู้ใช้] ใน dropdown
     - **ส่วนที่ 3:** ฟอร์มตรวจสอบ (ผู้ตรวจสอบ V001-V003)
     - **ส่วนที่ 4:** ตารางรายงานทั้งหมด (แสดงรหัสผู้ใช้แยกคอลัมน์)

3. **templates/summary.html**
   - หน้าสรุปผลและสถิติ
   - Dashboard 4 การ์ด:
     - ข่าวเข้าสู่ PANIC
     - ตรวจสอบแล้ว
     - รอการตรวจสอบ
     - โพสต์ข่าวทั้งหมด
   - ตารางรายละเอียดข่าว PANIC

4. **static/style.css**
   - Stylesheet หลัก
   - ใช้หลัก HCI: สีแยกประเภท (แดง=PANIC, เขียว=น่าเชื่อถือ, ส้ม=บิดเบือน)
   - Responsive Design
   - Hover Effects

---

### 🎮 CONTROLLER Layer (main.py)
**หน้าที่:** จัดการ Routing และประมวลผล Request

**ไฟล์:** `main.py` (ใช้โครงสร้าง MVC แบบโฟลเดอร์)

**Routes หลัก:**
1. `GET /` → `index()`
2. `GET /detail/<id>` → `detail()`
3. `POST /report/<id>` → `report_rumour()`
4. `POST /verify/<id>` → `verify_rumour()`
5. `GET /summary` → `summary()`

---

### ⚙️ CONFIG Layer (โฟลเดอร์ config/)
**หน้าที่:** จัดเก็บการตั้งค่าแบบรวมศูนย์

**ไฟล์:** `config/settings.py`
- `DATABASE_NAME = 'rumor_tracking.db'`
- `PANIC_THRESHOLD = 5` (จำนวนรายงานที่ทำให้เป็น PANIC)
- `SECRET_KEY`, `DEBUG`, `HOST`, `PORT`

---

## 🔄 การทำงานร่วมกันของ MVC

### Flow การรายงานข่าวลือ:
```
1. ผู้ใช้กรอกฟอร์มใน detail.html (VIEW)
2. ส่ง POST request ไปที่ /report/<id>
3. main.py (CONTROLLER) รับ request
4. ตรวจสอบ Business Rules (5 ข้อ)
5. เรียก ReportModel.create_report() (MODEL) บันทึกข้อมูล
6. เรียก RumourModel.update_credibility_score() (MODEL) คำนวณคะแนนใหม่
7. ตรวจสอบว่าถึง PANIC_THRESHOLD หรือยัง
8. ถ้าถึง เรียก RumourModel.update_status_to_panic() (MODEL)
9. Redirect กลับไปที่ detail.html (VIEW) พร้อม Flash Message
```

---

## 📋 b) Routes/Actions หลักและหน้าจอ View

### Route 1: GET / → index()
- **Controller:** `main.py` → `index()`
- **Model:** เรียก `RumourModel.get_all_rumours()`
- **View:** แสดงผลที่ `templates/index.html`
- **หน้าจอ:** รวมข่าวลือทั้งหมด เรียงตามความร้อนแรง

### Route 2: GET /detail/<rumour_id> → detail()
- **Controller:** `main.py` → `detail(rumour_id)`
- **Model:** 
  - `RumourModel.get_rumour_by_id()` - ดึงข้อมูลข่าว
  - `ReportModel.get_reports_by_rumour()` - ดึงรายงานทั้งหมด
  - `UserModel.get_all_users()` - ดึงผู้ใช้สำหรับ dropdown
  - `UserModel.get_verifiers()` - ดึงผู้ตรวจสอบ
- **View:** แสดงผลที่ `templates/detail.html`
- **หน้าจอ:** รายละเอียดข่าว + ฟอร์มรายงาน + ฟอร์มตรวจสอบ + ตารางรายงาน

### Route 3: POST /report/<rumour_id> → report_rumour()
- **Controller:** `main.py` → `report_rumour(rumour_id)`
- **Business Rules ที่ตรวจสอบ:**
  - Rule 4.1: ป้องกันการรายงานซ้ำ
  - Rule 4.2: PANIC Threshold (>= 5 รายงาน)
  - Rule 4.3: ห้ามรายงานข่าวที่ตรวจสอบแล้ว
  - Rule 4.5: คำนวณคะแนนอัตโนมัติ
- **Model:**
  - `ReportModel.check_duplicate_report()` - ตรวจสอบซ้ำ
  - `ReportModel.create_report()` - บันทึกรายงาน
  - `RumourModel.update_credibility_score()` - คำนวณคะแนนใหม่
  - `RumourModel.update_status_to_panic()` - อัปเดตสถานะถ้าถึง 5
- **View:** Redirect กลับ `detail.html` พร้อม Flash Message

### Route 4: POST /verify/<rumour_id> → verify_rumour()
- **Controller:** `main.py` → `verify_rumour(rumour_id)`
- **Business Rules ที่ตรวจสอบ:**
  - Rule 4.4: ข่าวหนึ่งข่าวตรวจสอบได้ครั้งเดียว
- **Model:**
  - `RumourModel.verify_rumour()` - บันทึกผลการตรวจสอบ
- **View:** Redirect กลับ `detail.html` พร้อม Flash Message

### Route 5: GET /summary → summary()
- **Controller:** `main.py` → `summary()`
- **Model:**
  - `RumourModel.get_panic_rumours()` - ดึงข่าว PANIC
  - `RumourModel.get_all_rumours()` - ดึงข่าวทั้งหมด
  - คำนวณสถิติ: verified_count, panic_count, pending_count
- **View:** แสดงผลที่ `templates/summary.html`
- **หน้าจอ:** Dashboard สถิติ + ตารางข่าว PANIC

---

## 📊 Business Rules ทั้ง 5 ข้อ

### Rule 4.1: ป้องกันการรายงานซ้ำ
```python
if ReportModel.check_duplicate_report(user_id, rumour_id):
    flash('คุณได้รายงานข่าวนี้ไปแล้ว', 'warning')
```

### Rule 4.2: PANIC Threshold
```python
if report_count >= 5 and rumour['status'] == 'ปกติ':
    RumourModel.update_status_to_panic(rumour_id)
```

### Rule 4.3: ห้ามรายงานข่าวที่ตรวจสอบแล้ว
```python
if rumour['is_verified']:
    flash('ข่าวนี้ถูกตรวจสอบแล้ว ไม่สามารถรายงานเพิ่มได้', 'warning')
```

### Rule 4.4: ข่าวหนึ่งข่าวตรวจสอบได้ครั้งเดียว
```python
if rumour['is_verified']:
    flash('ข่าวนี้ถูกตรวจสอบไปแล้ว', 'warning')
```

### Rule 4.5: คำนวณคะแนนอัตโนมัติ
```python
def calculate_credibility_score(rumour_id):
    total = COUNT(*) FROM Report WHERE rumour_id = ?
    credible = COUNT(*) WHERE report_type = 'น่าเชื่อถือ'
    return (credible / total) × 100

# เรียกทุกครั้งหลัง create_report()
RumourModel.update_credibility_score(rumour_id)
```

---

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง Users (13 คน)
- 10 ผู้ใช้ทั่วไป: user001, user002, ..., user010
- 3 ผู้ตรวจสอบ: verifier001 (V001), verifier002 (V002), verifier003 (V003)

### ตาราง Rumour (10 ข่าว)
- เก็บข้อมูล: title, content, source, credibility_score, status, is_verified

### ตาราง Report
- เก็บการรายงาน: user_id, rumour_id, report_type, report_date
- มี Foreign Keys เชื่อมกับ Users และ Rumour

---

## ✨ ฟีเจอร์พิเศษ

1. **คำนวณคะแนนความน่าเชื่อถืออัตโนมัติ**
   - ใช้ข้อมูลจริงจากฐานข้อมูล
   - สูตร: (จำนวนรายงาน "น่าเชื่อถือ" / จำนวนรายงานทั้งหมด) × 100
   - อัปเดตทุกครั้งที่มีรายงานใหม่

2. **แสดงรหัสผู้ใช้ทุกจุด**
   - Dropdown: `[user001] ชัยยา แซ่ตัน (ผู้ใช้ทั่วไป)`
   - ตารางรายงาน: มีคอลัมน์ "รหัสผู้ใช้" แยกต่างหาก

3. **ผู้ใช้งาน 13 คน**
   - 10 ผู้ใช้ทั่วไป (user001-user010)
   - 3 ผู้ตรวจสอบ (V001-V003)

4. **HCI Design**
   - สีแยกประเภท: แดง=PANIC, เขียว=น่าเชื่อถือ, ส้ม=บิดเบือน
   - Flash Messages ทุกการทำงาน
   - Responsive Layout

---

## 🚀 วิธีติดตั้งและใช้งาน

### 1. Clone Repository
```bash
git clone https://github.com/kmitlProjects/MVC_2568_2_q1.git
cd MVC_2568_2_q1
```

### 2. สร้าง Virtual Environment
```bash
python -m venv .venv
source .venv/bin/activate  # macOS/Linux
```

### 3. ติดตั้ง Flask
```bash
pip install flask
```

### 4. สร้างฐานข้อมูล
```bash
python database.py
```

### 5. รันเซิร์ฟเวอร์
```bash
python main.py
```

### 6. เปิดเว็บบราวเซอร์
- หน้าหลัก: http://127.0.0.1:5000/
- หน้าสรุปผล: http://127.0.0.1:5000/summary

---

## 📸 ภาพหน้าจอ

(แนบภาพหน้าจอ 3 ภาพตามที่อาจารย์กำหนด)

1. **หน้าแรก (index.html)** - แสดงข่าวลือทั้งหมด พร้อม Badge และคะแนน
2. **หน้ารายละเอียด (detail.html)** - แสดงฟอร์มรายงาน + ตารางรายงาน + รหัสผู้ใช้
3. **หน้าสรุปผล (summary.html)** - Dashboard 4 การ์ด + ตารางข่าว PANIC

---

## 🎯 สรุป

ระบบนี้ใช้โครงสร้าง **MVC แบบ Modular** ที่แยก Layer ชัดเจน:
- **Model** (models/) - จัดการข้อมูลและ Business Logic
- **View** (templates/) - แสดงผล UI
- **Controller** (main.py) - จัดการ Routing
- **Config** (config/) - การตั้งค่าแบบรวมศูนย์

ครบถ้วนตามข้อกำหนด Exit Exam MVC 2-2568 ทุกข้อ ✅
